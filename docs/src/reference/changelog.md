# Changelog

<!-- Single source of truth lives at the repo root. mdbook's
{{#include}} pulls the file contents in verbatim so this page
auto-updates when CHANGELOG.md is edited — no double-handling. -->

{{#include ../../../CHANGELOG.md}}
