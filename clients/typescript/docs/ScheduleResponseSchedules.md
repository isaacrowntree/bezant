
# ScheduleResponseSchedules

Returns a series of date objects of the given contract.

## Properties

Name | Type
------------ | -------------
`date` | [ScheduleResponseSchedulesDate](ScheduleResponseSchedulesDate.md)

## Example

```typescript
import type { ScheduleResponseSchedules } from 'bezant-client'

// TODO: Update the object below with actual values
const example = {
  "date": null,
} satisfies ScheduleResponseSchedules

console.log(example)

// Convert the instance to a JSON string
const exampleJSON: string = JSON.stringify(example)
console.log(exampleJSON)

// Parse the JSON string back to an object
const exampleParsed = JSON.parse(exampleJSON) as ScheduleResponseSchedules
console.log(exampleParsed)
```

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)


